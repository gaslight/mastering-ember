<div class="row">
  <div class="col-md-8">
    <form>
      <div class="form-group">
        <label>Name</label>
        {{input class='form-control' value=model.name}}
      </div>
      <div class="form-group">
        <label>Artwork URL</label>
        {{input class='form-control' value=model.artworkUrl}}
      </div>

      <button {{action "create"}}>Create</button>
    </form>
  </div>

</div>
